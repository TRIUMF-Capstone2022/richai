dataset:
  base_dir: "/data/bvelghe/capstone2022/"
  train: ["B", "C"]
  test: ["A"]

data_loader:
  batch_size: 64
  num_workers: 8  

model:
  pointnet:
    num_classes: 3
    epochs: 5
    saved_model: "saved_models/pointnet_momentum.pt"
    predictions: "saved_models/predictions_momentum.csv"

gpu:
  - 0
  - 1
  - 2
  - 3
  - 4
  - 5
  - 6
  - 7