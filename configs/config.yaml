dataset:
  base_dir: "/data/bvelghe/capstone2022/"
  train: ["B", "C"]
  test: ["A"]

data_loader:
  batch_size: 64
  num_workers: 8  

model:
  pointnet:
    num_classes: 3
    saved_model: "saved_models/pointnet.pt"
